# Instructions

Make a directory named distros, and a subdirectory matching the name of the distro you want to virtualize. Then put your ISO file inside of there. Enter the makefile and specify that path for your ISO variable. For the IMAGE variable, just name the location you want your qcow2 (virtual disk storage) to go.
