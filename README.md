# Instructions

Make a directory for your ISO file to go, and specify that directory as the ISO variable in the makefile. Then set your IMAGE variable also in your makefile to the path for your virtual disk storage (.qcow2)
